apiVersion: v1
kind: Service
metadata:
  name: {{ include "artifactory-edge-exporter.fullname" . }}
  labels:
    {{- include "artifactory-edge-exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "artifactory-edge-exporter.selectorLabels" . | nindent 4 }}
